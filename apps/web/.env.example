# Task 10.1 (Foundation). Base URL of apps/api (the gateway) -- the ONLY
# backend address apps/web ever talks to. No broker, service-specific, or
# per-domain URLs belong here; everything goes through this one gateway via
# @tradosphere/sdk (see src/lib/sdk.ts).
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
